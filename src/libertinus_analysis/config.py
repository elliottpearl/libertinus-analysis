from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent.parent
FONTS_DIR = ROOT / "fonts"
DATA_DIR = ROOT / "data"
FONTDATA = DATADIR / "fontdata"
DIAGNOSTICS = DATA_DIR / "diagnostics"
TEX_DIR = ROOT / "tex"
TEX_INPUT = TEX_DIR / "input"
