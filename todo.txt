## PLAN

### 1. Remove `is_italic_slanted` from per‑glyph tags  
- Delete its computation in `compute_semantic_tags()`.  
- Remove it from the returned dict.  
- Update `SCHEMA.md` to reflect the reduced tag set.

### 2. Add a font‑level slant flag in the JSON root  
- In `extract_fontmetrics()`, add:

```
"font_style": style,
"is_italic_slanted": (style contains "italic" or "oblique")
```

- This appears once per JSON file, not per glyph.

### 3. Update `ANCHOR-HEURISTICS.md`  
- Move italic correction out of the per‑glyph tag logic.  
- State clearly that italic correction is a **font‑level** adjustment applied uniformly.

### 4. Update the anchor‑placement code (later)  
- When computing `dx_norm`, read the font‑level `is_italic_slanted`.  
- Apply the slant correction once per glyph, but based on the font‑level flag.

### 5. Regenerate fontmetrics JSON  
- Run your existing build script.  
- Verify that the new tag set is clean and that the font‑level flag appears correctly.
